name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: TAG=$(git rev-parse --short HEAD) docker-compose -f .docker/docker-compose.yml build
